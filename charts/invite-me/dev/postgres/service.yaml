# PostgreSQL StatefulSet Service
apiVersion: v1
kind: Service
metadata:
  name: psql
  namespace: invite-me-dev
spec:
  selector:
    app: postgresql-db
  type: LoadBalancer
  ports:
    - name: postgres-port
      port: 5432
      targetPort: 5432
      protocol: TCP