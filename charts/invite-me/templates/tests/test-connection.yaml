apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "invite-me.fullname" . }}-test-connection"
  labels:
    {{- include "invite-me.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "invite-me.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
