apiVersion: v1
kind: Service
metadata:
  name: {{ include "invite-me.fullname" . }}
  labels:
    {{- include "invite-me.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "invite-me.selectorLabels" . | nindent 4 }}
